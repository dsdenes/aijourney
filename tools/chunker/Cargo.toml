[package]
name = "chunker"
version = "0.1.0"
edition = "2024"
description = "Fast text chunker for RAG pipeline — splits documents into overlapping semantic chunks"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
